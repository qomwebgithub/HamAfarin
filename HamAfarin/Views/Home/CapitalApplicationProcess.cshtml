@model ViewModels.FooterViewModel

@{
    ViewBag.Title = "فرایند درخواست سرمایه";
}

<!-- slider start -->
@Html.Action("OtherPageSlider", "Slider", new { sliderPageId = (int)SliderPages.CAMPAIGN_PROCESS })
<!-- slider end -->
<div class="container">

    <h2 class="m-4">فرایند درخواست سرمایه</h2>

    <div>
        <hr />
        <div class="container m-4">
            <dl class="dl-horizontal">


                <dd>
                    @Html.Raw(@Model.CapitalApplicationProcess)
                </dd>
            </dl>
        </div>

    </div>
</div>
